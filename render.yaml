services:
  - type: web
    name: ivoirerental
    runtime: static
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @workspace/ivoir-rental run build
    staticPublishPath: artifacts/ivoir-rental/dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
